# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Pericode (PP Edition) (for example via API key or ChatGPT auth supported by Codex).
- Pericode (PP Edition) starts the server via `codex app-server` per session.
